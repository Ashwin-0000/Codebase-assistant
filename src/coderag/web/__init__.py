"""
web — FastAPI-based Web UI for CodeRAG.

Start with::

    coderag serve                       # auto-opens the browser
    uvicorn coderag.web.app:app --reload  # manual dev server

Install the optional web extras first::

    pip install 'coderag[web]'
"""
